---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: csi-attacher-sdafs-sa
  namespace: kube-system

---
kind: ClusterRole
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  name: csi-attacher-sdafs-cr
rules:
  - apiGroups: [""]
    resources: ["secrets"]
    verbs: ["get", "list"]
  - apiGroups: [""]
    resources: ["events"]
    verbs: ["get", "list", "watch", "update", "patch", "create"]
  - apiGroups: [""]
    resources: ["persistentvolumes"]
    verbs: ["get", "list", "watch", "update", "patch", "create", "delete"]
  - apiGroups: [""]
    resources: ["persistentvolumeclaims"]
    verbs: ["get", "list", "watch", "update", "patch"]
  - apiGroups: ["storage.k8s.io"]
    resources: ["storageclasses"]
    verbs: ["get", "list", "watch", "update"]
  - apiGroups: [""]
    resources: ["nodes"]
    verbs: ["get", "list", "watch"]
  - apiGroups: ["storage.k8s.io"]
    resources: ["csinodes"]
    verbs: ["get", "list", "watch"]
  - apiGroups: ["storage.k8s.io"]
    resources: ["volumeattachments", "volumeattachments/status"]
    verbs: ["get", "list", "watch", "update", "patch"]
  - apiGroups: ["coordination.k8s.io"]
    resources: ["leases"]
    verbs: ["get", "list", "watch", "update", "patch", "create"]

---
kind: ClusterRoleBinding
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  name: csi-attacher-role-binding
subjects:
  - kind: ServiceAccount
    name: csi-attacher-sdafs-sa
    namespace: kube-system
roleRef:
  kind: ClusterRole
  name: csi-attacher-sdafs-cr
  apiGroup: rbac.authorization.k8s.io

---
kind: DaemonSet
apiVersion: apps/v1
metadata:
  name: csi-attacher-sdafs
  namespace: kube-system
  annotations:
    checkov.io/skip1: CKV_K8S_9=No good probes available
    checkov.io/skip2: CKV_K8S_8=No good probes available
    checkov.io/skip3: CKV_K8S_38=Is this probe broken?
    checkov.io/skip4: CKV_K8S_43=Prefer non-digests here
spec:
  selector:
    matchLabels:
      app: csi-sdafs
      role: systemservices
  template:
    metadata:
      labels:
        app: csi-sdafs
        role: systemservices
    spec:
      serviceAccount: csi-attacher-sdafs-sa
      automountServiceAccountToken: true
      securityContext:
        seccompProfile:
          type: RuntimeDefault
        runAsUser: 65534
        runAsGroup: 65535
      tolerations:
        - key: node-role.kubernetes.io/master
          operator: "Exists"
      containers:
        - name: csi-attacher
          image: {{ .Values.attacher.image }}
          args:
            - "--v=4"
            - "--csi-address=$(ADDRESS)"
            - "--leader-election"
            - "--http-endpoint=:4002"
          env:
            - name: ADDRESS
              value: /var/lib/kubelet/plugins/csi.sda.nbis.se/csi.sock
          imagePullPolicy: {{ .Values.attacher.pullPolicy }}
          volumeMounts:
            - name: socket-dir
              mountPath: /var/lib/kubelet/plugins/csi.sda.nbis.se
          resources:
            requests:
              memory: "{{ .Values.resources.attacher.requests.memory }}"
              cpu: "{{ .Values.resources.attacher.requests.cpu }}"
            limits:
              memory: "{{ .Values.resources.attacher.requests.memory }}"
              cpu: "{{ .Values.resources.attacher.requests.cpu }}"
              
          securityContext:
            runAsNonRoot: true
            readOnlyRootFilesystem: true
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - all
        - name: csi-provisioner
          image: {{ .Values.provisioner.image }}
          args:
            - "--csi-address=$(ADDRESS)"
            - "--v=4"
            - "--leader-election"
            - "--http-endpoint=:4001"
            - "--controller-publish-readonly=true"
            - "--node-deployment=true"
          env:
            - name: ADDRESS
              value: /var/lib/kubelet/plugins/csi.sda.nbis.se/csi.sock
            - name: NODE_NAME
              valueFrom:
                fieldRef:
                  fieldPath: spec.nodeName
          imagePullPolicy: {{ .Values.provisioner.pullPolicy }}
          volumeMounts:
            - name: socket-dir
              mountPath: /var/lib/kubelet/plugins/csi.sda.nbis.se
          resources:
            requests:
              memory: "{{ .Values.resources.provisioner.requests.memory }}"
              cpu: "{{ .Values.resources.provisioner.requests.cpu }}"
            limits:
              memory: "{{ .Values.resources.provisioner.limits.memory }}"
              cpu: "{{ .Values.resources.provisioner.limits.cpu }}"
          securityContext:
            runAsNonRoot: true
            readOnlyRootFilesystem: true
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - all
      volumes:
        - name: socket-dir
          hostPath:
            path: /var/lib/kubelet/plugins/csi.sda.nbis.se
            type: DirectoryOrCreate
 